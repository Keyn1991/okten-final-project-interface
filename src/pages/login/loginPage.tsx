import React from 'react';
import FormLogin from "../../Components/FormLogin/FormLogin";

const LoginPage = () => {
    return (
        <div>
            <FormLogin/>
        </div>
    );
};

export default LoginPage;